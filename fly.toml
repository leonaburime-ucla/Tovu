# ============================================================================
# Remote corrected 2026-09-09 — the paragraph below was stale and has misled a session before.
#
# Production (`tovu.fly.dev`) is deployed by the "Deploy to Fly.io" workflow
# (`.github/workflows/fly-deploy.yml`), triggered on push to `main`. This checkout's only
# remote is now `origin` -> `github.com/AINSEP/Tovu.git` (transferred 2026-09-23 from
# `leonaburime-ucla/Tovu`; old URL 301-redirects) — the SAME public repo
# that deploy runs from, not a separate private working repo. `gh run list` / `gh api
# repos/:owner/:repo/...` run from this directory therefore already answer about the right
# repo; no `--repo` override is needed.
#
# What is still true and still worth knowing: this file (and the workflow itself) live only
# on `restructure/apps-website-phased` today, not on `main` (the branch the workflow's
# `push:` trigger actually watches) — see that workflow file's own header for the current
# commit-count gap and how to fire a deploy by hand (`workflow_dispatch`) in the meantime.
#
# `app` matches the live app: `tovu` resolves (HTTP 200); an earlier `tovu-ai-cms` did not
# (HTTP 000) and was corrected below to match observed production.
# ============================================================================

# Tovu — Fly.io app config.
#
# This file lives at the Tovu repo root, next to `Dockerfile` and
# `docker-compose.yml`. `@jini-ai/*` packages are consumed from the npm registry, so the image
# builds from this checkout alone — no sibling `Jini` tree, no special build-context handling.
# `flyctl deploy` run bare from the Tovu repo root (or via
# `development/scripts/fly-build.sh`, which just pins the context to this repo's root
# regardless of the caller's cwd) is enough:
#
#     flyctl deploy
#
# This file's own `[build] dockerfile = "Dockerfile"` is already correct relative to that
# default context. A remote builder (Depot, Fly's own) works too now: flyctl uploads this
# repo's own local build context, and that context is self-contained.
#
# ============================================================================
# Fields below this line are a STARTING POINT, not a verified config.
# ============================================================================
# `app` matches the name already used in prior deploy attempts
# (`tovu-ai-cms`). Everything else — region, volume name/size, machine
# sizing — is inferred from the Dockerfile's own documented runtime
# requirements (EXPOSE 3000, VOLUME /workspace/Tovu/sites) or left as an
# explicit placeholder. Confirm against `flyctl config show -a tovu`
# if an app by that name already exists on Fly before trusting this file, and
# update the placeholders below rather than assuming they're right.

app = "tovu"
primary_region = "iad" # PLACEHOLDER — confirm the intended region.

[build]
  dockerfile = "Dockerfile"

[env]
  # Non-secret only. Real secrets (TOVU_ADMIN_PASSWORD, TOVU_ADMIN_USER,
  # TOVU_INTEGRATIONS_ROOT_KEY — see docker-compose.yml's own comments for
  # what each does) go through `fly secrets set`, never in this committed
  # file.
  TOVU_RUNTIME_MODE = "production"
  PORT = "3000"

  # The live site's folder on the `tovu_sites` volume below. Pinned 2026-09-26 when the repo's own
  # site folder was renamed `sites/tovu-com` -> `sites/tovu-dev` (which also changed the code default,
  # `DEFAULT_SITE_NAME`): the volume still holds `tovu-com/`, and without this pin the next deploy
  # would boot an empty `tovu-dev/` next to it. To rename the live folder too, stop the machine,
  # `mv /workspace/Tovu/sites/tovu-com /workspace/Tovu/sites/tovu-dev` on the volume, then drop this line.
  TOVU_SITE = "tovu-com"

  # This deployment's public origin. NOT a secret (it is the URL crawlers already see), so it
  # belongs here rather than in `fly secrets set`.
  #
  # Load-bearing, added 2026-09-18 — see
  # `ADS-memory/reports/2026-09-18-public-origin-registration-design.md`. This is the ONLY input by
  # which a real public origin can enter the ADR-040 origin registry
  # (`features/origin/configured-origin.ts` reads it at boot;
  # `platform/db/sqlite/origin-repo.sqlite.ts`'s `registerConfiguredOrigin` writes it). With it
  # unset, a production boot registers no origin at all and every public URL degrades to a
  # relative path: `sitemap.xml`'s `<loc>`, `robots.txt`'s `Sitemap:` line, `canonical`, `og:url`,
  # `og:image`, plus newsletter confirm/unsubscribe and member magic links. The sitemap protocol
  # requires absolute `<loc>` values, so leaving this unset is a real (if safe) SEO regression.
  #
  # Setting it also corrects the `http://localhost:3000` row that Fly's FIRST boot durably wrote
  # into this app's `content.db` (the bug `d21d6e7ac` patched defensively on the read side). That
  # correction happens on the next boot after this value is in place — no migration needed.
  #
  # Must be an `https` URL with a non-loopback host, or it is refused with a warning in the boot
  # log and no origin is registered. The same variable already drives OAuth callback URLs and
  # admin deep links, deliberately reused rather than duplicated (ADR-040 names duplicated origin
  # settings as a fracture it exists to close).
  TOVU_PUBLIC_URL = "https://tovu.fly.dev"

# Persistent state: every site's content.db + WAL sidecars, uploads/,
# themes/, ops state — everything else in the image is disposable. A missing
# or misconfigured mount here is silent data loss on next deploy, not a
# startup error. PLACEHOLDER volume name/size — create it first with
# `fly volumes create tovu_sites --region <region> -a tovu` and match
# the name here.
[[mounts]]
  source = "tovu_sites"
  destination = "/workspace/Tovu/sites"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  # `/readyz` exists (apps/website/src/server/inbound/public-http/routes/ops/health.ts,
  # wired via registerReadyzRoute) and is 503 until migrations/seeding finish
  # — a truer "actually ready" signal than `/health`, which is liveness-only.
  [[http_service.checks]]
    grace_period = "60s"
    interval = "30s"
    method = "GET"
    timeout = "5s"
    path = "/readyz"
